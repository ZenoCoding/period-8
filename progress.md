Original prompt: we need to fix the main gameplay loop. 

Notes:
- Current issue: transition signs are visible before the player is irreversibly in the next loop, then the game snaps the player back to the canonical start.
- Agreed direction: commit earlier, show the result sign after commit, block returning past the commit gate, and stream a next hallway ahead of the transition so the handoff is visually continuous.
- Current implementation chunk: add tunable commit/sign placement and use those values in the live transition loop.
- Implemented design in progress: next hallway is queued beyond the chosen transition after commit, commit/sign values are loaded from localStorage, and debug mode can capture commit/sign positions in-game.
- Follow-up fix: queued hallway is now an entry preview only, aligned by the transition exit to the preview's open mouth. It no longer adds a second active light rig while waiting for handoff.
- Correction after manual QA: preview approach was replaced with a queued full hallway variant. The transition endpoint is now exported from hallway geometry and shortened from 25.6 to 18.2 so the old final straight does not append a second full straightaway before the new hallway.
- Follow-up: handoff now waits until queued local z < 2.1 instead of z < 7.1, so the entrance-side branch/opening is behind the player before the full hallway replaces the queued version.
- Correction: visual overlap at the transition entry was removed. The queued hallway now renders exactly at the shared transition endpoint, while collision gets a hidden positive-z extension to prevent the player-radius seam from becoming an invisible wall. Handoff moved back earlier to z < 6.25.
- Topology fix: removed the final straight tail from transition geometry. The branch now ends on the same horizontal connector shape used by normal hallway-to-transition joins, and the queued hallway root is placed at (+/-16, +/-24) so its omitted entrance branch exactly occupies that connector.
- Reverted bad topology fix after screenshot showed a broken side connector. Restored the original end corridor and horizontal frame. Queued hallway now aligns its local z=+8 end to the exported transition endpoint at z=25.6, with only a collision extension at the queued main hallway's positive-z end.
- Restored the side-connector approach correctly: transition geometry now ends on the side connector rect again, queuedNext opens the main hallway's positive side where the omitted branch would attach, queued root is placed at (+/-19, +/-24), and collision tolerance extends on local +X instead of +Z.
- Corrected the side connector miss: queued root x is now +/-14.5 so the omitted positive branch exactly covers the old connector, the new main overlaps the connector at x 13..16 / z 16..17.5, and the connector opens on that side overlap instead of carving a front-facing exit. Removed the hidden collision extension because the rect overlap is now real.
- Shifted the queued hallway handoff earlier from local z < 6.25 to z < 7.25 so the scene swap happens inside the connector overlap before the player is looking straight down the new hallway.
- Updated baked transition tuning from manual captures: positive sign defaults to x 9.43 / z 13.10 / rot -1.57 with mirrored negative placement. Bumped tuning storage to v2 so the new defaults apply. Handoff now triggers around queued-local z < 8.15 while still in the entrance branch, with same-side transition re-entry suppressed until the player reaches the main hallway.
- Geometry scale pass: hallway root now renders at 1.2x scale, the main hallway local half-length increased from 8 to 12 for a 1.5x longer straight, and transition/queued-root constants are derived from those dimensions. Player start, speed, exit checks, and recentering now convert between scaled world coordinates and hallway-local gameplay coordinates.
- Cross-section pass: local main hallway half-width increased from 1.5 to 1.8 and wall height from 3.0 to 3.6. Transition openings/connectors derive from the new width, and wall/ceiling-mounted props were moved to the new side wall and ceiling positions.
- Removed the redundant global 1.2x root scale after realizing it scaled the player/collision with the hallway. The effective geometry changes now come from local dimensions only: half-length 12, half-width 1.8, wall height 3.6.

- New goal on 2026-05-14: upgrade hallway to a semi-realistic school corridor without jank; prefer CC0 online assets for broad materials, procedural/detail geometry where assets would be heavier than useful, and verify with screenshots.

- Material QA: first screenshot was too glossy and visibly tiled; reduced floor gloss/scuff opacity and switched plan toward cleaner plaster because repeated cracks looked artificial on long walls.

- Implemented semi-realistic material pass: CC0 ambientCG Plaster001, Tiles107, Wood095, and Metal032 maps are loaded through a cached PBR helper; box UVs now scale by world dimensions; floor grid lines were replaced with tile material plus subtle procedural wear.
- Added geometry/detail polish: ceiling rails, gentler scuff decals, upgraded fluorescent fixture materials with shadow spotlights, wood/metal classroom doors with kick plates/hinges/window frames, and more detailed lockers.
- Verification: npm run build passed, npm test passed, built-in Browser loaded http://127.0.0.1:5175/ with no console warnings/errors. Built-in screenshot capture timed out on the WebGL canvas, so output/web-game/shot-0.png was captured with the web-game client and visually inspected after each tuning pass.
- Current dev server is running on http://127.0.0.1:5175/.

- Asset goal on 2026-05-14: imported classroom door, locker bank/door, and wall clock face assets should appear in the hallway while existing anomaly handles still manipulate owned pivots.
- Implemented asset import path: added scripts/generateHallwayAssets.mjs to generate GLTF assets in public/models/hallway and added GLTFLoader caching/runtime mounting with procedural fallbacks.
- Imported assets now appear in-game for classroom doors, locker bank + separate anomaly door, and clock face. Locker anomaly rotates the owned locker-door-anomaly-pivot; clock anomaly still uses owned hand pivots over the imported face.
- Online asset pass: replaced the primary generated classroom door, locker bank, and clock face loads with downloaded GLB assets from Get3DModels, added runtime scale/orientation normalization, and kept generated/procedural props as fallbacks/reference assets.
- Visual correction after screenshot QA: stopped loading the generated single locker-door GLTF because it rendered as a black slab over the locker bank. The imported locker bank is now the normal-state read, while a small purpose-built door/interior only appears for the locker anomaly. The downloaded clock GLB was the wrong side-profile style, so the runtime now uses a custom round school wall clock mesh with controllable hands.
- Floor texture correction: floor/ceiling surfaces now render from non-overlapping union cells instead of overlapping hallway boxes, and floor UVs are derived from shared world coordinates so tile grout lines align across transition connector pieces. Removed the per-rect floor-wear overlay because it created repeated/double-dark marks at overlaps.
- Audio pass: expanded the existing procedural horror audio with actual movement-speed-driven footsteps, fluorescent ballast buzz/flicker, and a generated room reverb send so walking has a larger, more eerie corridor tail without adding external audio files.
- Follow-up audio asset pass: added the user-provided Pixabay/Freesound hallway footsteps MP3 under `public/audio/footsteps/` and wired the footstep player to use short recorded slices with the existing room/echo sends, falling back to generated noise if the MP3 has not decoded yet.
- Footstep slice timing correction: analyzed the MP3 waveform for transient footfall peaks and replaced the guessed fixed-stride offset with 18 hardcoded slice starts just before the detected impacts.
- Footstep loudness correction: boosted recorded-slice gain, wet sends, and the walker distance falloff so the hallway footsteps should read clearly instead of being buried under the room tone.
- Double-hit correction: recorded slices now start closer to the detected impact, use a shorter 0.32s window, and mute the synthetic heel thump for recorded footsteps so each trigger reads as one step instead of a gallop.
- Debug simplification after double-hit persisted: recorded footsteps now bypass filter, pitch variation, stereo pan, synthetic heel, room send, and echo send; each trigger plays a short 0.2s dry MP3 slice only.
- Footstep source isolation: disabled the procedural fallback entirely for footsteps and switched recorded playback to one fixed 0.2s slice at 4.77s, so alternating deep/light steps cannot come from rotating through different MP3 offsets or from the old generated sound.
- Raw segment correction: confirmed the shipped MP3 matches the Downloads file byte-for-byte, cache-busted the fetch URL, and replaced the tiny fixed debug slice with longer 0.52s raw segments that cycle through the recording.
- Moderate effects pass: kept recorded MP3 as the only footstep source, then added light stereo pan plus modest room and slower/lower echo sends so the hallway space returns without sounding like an extra step.
- Walker sync/direction pass: sped the business-man walk clip from 0.82x to 1.18x, exposed animation-phase footstep pulses from the walker, made NPC footsteps fire from those pulses instead of a separate timer, and changed NPC pan to listener-relative camera yaw with distance-based volume/wetness.
- Verification preference from user on 2026-05-19: do not use Playwright for this project; leave browser/game feel checks to manual verification unless explicitly asked otherwise.
- Performance pass on 2026-05-19: target render cadence is now capped at 30fps, retina pixel ratio is capped at 1.35, static shadow maps no longer update every frame, queued hallway lights/shadows are reduced, and transition handoff reuses a standby hallway instead of building/disposing scene graphs at the swap.
- Success-sign freeze follow-up: hidden queued/standby signs are no longer repainted when the visible success/error sign appears, and standby hallway state prep is scheduled in idle time after commit with a synchronous fallback only if the player reaches handoff before idle prep completes.
- FPS counter added to the HUD: top-left display shows averaged FPS and the latest wall-clock frame time, so a transition stall should show as a high millisecond frame even with simulation deltas clamped.
- Verification: npm test passed and npm run build passed. Per user preference, no browser/manual verification was completed.
- Screen-weirdness pass on 2026-05-25: added a persistent low-level lens warp/chromatic fuzz shader, CSS scanline/noise overlay, tiny FOV/roll/jitter camera drift, and short transition/anomaly pulses. This is intentionally not strongly tied to ambience level; manual feel tuning should focus on `BASE_SCREEN_FUZZ`, `BASE_LENS_WARP`, and `updateScreenTreatment` in `src/render/app/createRepetitionGame.ts`.
- Ambience smoothing pass on 2026-05-25: atmosphere and audio now consume a shared smoothed ambience value instead of raw `state.ambienceLevel`, so wrong-choice increases and escape resets ease over time. `render_game_to_text` exposes both raw `ambienceLevel` and `visibleAmbienceLevel`.
- Screen-pass brightness correction: the weird-screen render target now stays in linear color and the final shader applies Three's tone mapping/output conversion, restoring the hallway baseline brightness while keeping the distortion/noise pass.
- Screen-pass black-frame fix: removed duplicate GLSL tone/color pars includes from the custom ShaderMaterial; Three injects those declarations automatically, while the shader still calls the final tone-mapping and colorspace fragments.
- Screen movement tuning: reduced camera shake, roll, FOV breathing, CSS overlay translation, and shader drift so the weird-screen pass reads more like texture/fuzz than visible camera motion.
