Original prompt: we need to fix the main gameplay loop. 

Notes:
- Current issue: transition signs are visible before the player is irreversibly in the next loop, then the game snaps the player back to the canonical start.
- Agreed direction: commit earlier, show the result sign after commit, block returning past the commit gate, and stream a next hallway ahead of the transition so the handoff is visually continuous.
- Current implementation chunk: add tunable commit/sign placement and use those values in the live transition loop.
- Implemented design in progress: next hallway is queued beyond the chosen transition after commit, commit/sign values are loaded from localStorage, and debug mode can capture commit/sign positions in-game.
- Follow-up fix: queued hallway is now an entry preview only, aligned by the transition exit to the preview's open mouth. It no longer adds a second active light rig while waiting for handoff.
- Correction after manual QA: preview approach was replaced with a queued full hallway variant. The transition endpoint is now exported from hallway geometry and shortened from 25.6 to 18.2 so the old final straight does not append a second full straightaway before the new hallway.
- Follow-up: handoff now waits until queued local z < 2.1 instead of z < 7.1, so the entrance-side branch/opening is behind the player before the full hallway replaces the queued version.
- Correction: visual overlap at the transition entry was removed. The queued hallway now renders exactly at the shared transition endpoint, while collision gets a hidden positive-z extension to prevent the player-radius seam from becoming an invisible wall. Handoff moved back earlier to z < 6.25.
- Topology fix: removed the final straight tail from transition geometry. The branch now ends on the same horizontal connector shape used by normal hallway-to-transition joins, and the queued hallway root is placed at (+/-16, +/-24) so its omitted entrance branch exactly occupies that connector.
- Reverted bad topology fix after screenshot showed a broken side connector. Restored the original end corridor and horizontal frame. Queued hallway now aligns its local z=+8 end to the exported transition endpoint at z=25.6, with only a collision extension at the queued main hallway's positive-z end.
- Restored the side-connector approach correctly: transition geometry now ends on the side connector rect again, queuedNext opens the main hallway's positive side where the omitted branch would attach, queued root is placed at (+/-19, +/-24), and collision tolerance extends on local +X instead of +Z.
- Corrected the side connector miss: queued root x is now +/-14.5 so the omitted positive branch exactly covers the old connector, the new main overlaps the connector at x 13..16 / z 16..17.5, and the connector opens on that side overlap instead of carving a front-facing exit. Removed the hidden collision extension because the rect overlap is now real.
- Shifted the queued hallway handoff earlier from local z < 6.25 to z < 7.25 so the scene swap happens inside the connector overlap before the player is looking straight down the new hallway.
- Updated baked transition tuning from manual captures: positive sign defaults to x 9.43 / z 13.10 / rot -1.57 with mirrored negative placement. Bumped tuning storage to v2 so the new defaults apply. Handoff now triggers around queued-local z < 8.15 while still in the entrance branch, with same-side transition re-entry suppressed until the player reaches the main hallway.
- Geometry scale pass: hallway root now renders at 1.2x scale, the main hallway local half-length increased from 8 to 12 for a 1.5x longer straight, and transition/queued-root constants are derived from those dimensions. Player start, speed, exit checks, and recentering now convert between scaled world coordinates and hallway-local gameplay coordinates.
- Cross-section pass: local main hallway half-width increased from 1.5 to 1.8 and wall height from 3.0 to 3.6. Transition openings/connectors derive from the new width, and wall/ceiling-mounted props were moved to the new side wall and ceiling positions.
- Removed the redundant global 1.2x root scale after realizing it scaled the player/collision with the hallway. The effective geometry changes now come from local dimensions only: half-length 12, half-width 1.8, wall height 3.6.
